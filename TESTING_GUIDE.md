# テストガイド - Testing Guide

## 実装された機能のテスト方法

### 1. 自動テスト（Unit Tests）

#### すべてのテストを実行
```bash
npm run test:ci
```

#### インタラクティブなテストモード（ウォッチモード）
```bash
npm run test
```

#### テストUIで確認
```bash
npm run test:ui
```

#### カバレッジレポート
```bash
npm run test:coverage
```

### 2. 手動テスト（ブラウザでの確認）

#### 開発サーバーを起動
```bash
npm run dev
```

ブラウザで `http://localhost:3000` を開きます。

#### テストシナリオ

##### A. タイプアヘッド検索の最適化（デバウンス）
1. 検索フィールドに文字を入力
2. 入力中に検索結果が更新されることを確認
3. 150ms以内に応答することを確認（開発者ツールのNetworkタブで確認可能）

**テストケース:**
- 高速で文字を入力 → デバウンスが機能し、不要な検索が実行されない
- 入力後150ms待つ → 検索結果が更新される

##### B. 検索結果の順序付け（前方一致→部分一致→後方一致）

**テストケース 1: 前方一致**
- 検索: "交差点"
- 期待: "交差点での..." が最初に表示され、緑色の「前方」バッジが表示される

**テストケース 2: 部分一致**
- 検索: "歩行者"
- 期待: "交差点での歩行者と..." が表示され、黄色の「部分」バッジが表示される

**テストケース 3: 後方一致**
- 検索: "事故"
- 期待: "...との事故" が表示され、灰色の「後方」バッジが表示される

**テストケース 4: 複数マッチ**
- 検索: "交差点"
- 期待: 前方一致が最初、次に部分一致、最後に後方一致の順で表示される

##### C. 章・節ごとのヒット数バッジ表示

**テストケース:**
1. 検索フィールドに "交差点" と入力
2. 検索結果の上に章ごとのバッジが表示されることを確認
3. 各バッジに章タイトルとヒット数が表示されることを確認
   - 例: "交差点における事故 [3]"

**確認ポイント:**
- バッジが正しい章タイトルを表示している
- ヒット数が正確である
- 複数の章がある場合、すべての章のバッジが表示される

##### D. 全角/半角・かな/カナ正規化機能

**テストケース 1: 全角/半角の正規化**
- 全角で検索: "交差点"（全角スペース）
- 半角で検索: "交差点"（半角スペース）
- 期待: 同じ結果が返される

**テストケース 2: カタカナ/ひらがなの正規化**
- ひらがなで検索: "こうさてん"
- カタカナで検索: "コウサテン"
- 期待: 同じ結果が返される

**テストケース 3: 英数字の正規化**
- 全角英数字: "ＡＢＣ１２３"
- 半角英数字: "ABC123"
- 期待: 同じ結果が返される

**テストケース 4: 混合**
- 検索: "交差点 ＡＢＣ"
- 期待: 正規化されて検索される

##### E. 空表示の改善

**テストケース:**
1. 存在しないキーワードで検索（例: "存在しないキーワード"）
2. 空の結果メッセージが表示されることを確認
3. "フィルタを解除" ボタンが表示されることを確認
4. "フィルタを解除" ボタンをクリック → すべての結果が表示される

### 3. パフォーマンステスト

#### 開発者ツールを使用
1. ブラウザの開発者ツールを開く（F12）
2. Performance タブを開く
3. 記録を開始
4. 検索フィールドに文字を入力
5. 記録を停止
6. P95レイテンシが150ms以下であることを確認

#### コンソールでタイミングを確認
ブラウザのコンソールで以下を実行:
```javascript
// 検索のパフォーマンスを測定
const start = performance.now();
// 検索を実行
const end = performance.now();
console.log(`Search took ${end - start}ms`);
```

### 4. 統合テスト（MCP Tests）

#### 自動MCPテスト
```bash
npm run test:mcp:comprehensive
```

#### 基本的なMCPチェック
```bash
npm run test:mcp:check
```

### 5. ビルドテスト

#### 本番ビルドの確認
```bash
npm run build
```

エラーなくビルドが完了することを確認します。

### 6. 具体的なテストシナリオ例

#### シナリオ 1: 基本的な検索フロー
1. アプリを開く
2. ステップ1の検索フィールドに "交差点" と入力
3. 章バッジが表示されることを確認
4. 検索結果が前方一致→部分一致→後方一致の順で表示されることを確認
5. 各結果にマッチタイプバッジが表示されることを確認
6. 結果をクリックして選択できることを確認

#### シナリオ 2: 正規化の確認
1. 検索フィールドに "コウサテン"（カタカナ）と入力
2. "交差点" の結果が表示されることを確認
3. 検索フィールドをクリア
4. "こうさてん"（ひらがな）と入力
5. 同じ結果が表示されることを確認

#### シナリオ 3: デバウンスの確認
1. 検索フィールドに高速で "交" → "交差" → "交差点" と入力
2. 開発者ツールのNetworkタブを確認
3. 不要なAPIリクエストが発生していないことを確認
4. 最終的な検索結果が正しく表示されることを確認

#### シナリオ 4: 空結果の確認
1. 検索フィールドに "存在しないキーワード" と入力
2. "検索結果が見つかりませんでした" が表示されることを確認
3. "フィルタを解除" ボタンが表示されることを確認
4. "フィルタを解除" をクリック
5. すべての結果が表示されることを確認

### 7. テスト結果の確認ポイント

✅ **正常動作の確認:**
- [ ] 検索が150ms以内に応答する
- [ ] 前方一致が最初に表示される
- [ ] 章バッジが正しく表示される
- [ ] 全角/半角、かな/カナが正規化される
- [ ] 空結果時に適切なメッセージが表示される
- [ ] デバウンスが機能している
- [ ] すべてのユニットテストが通過する

❌ **エラーの確認:**
- [ ] コンソールにエラーが表示されない
- [ ] 検索結果が正しくソートされている
- [ ] バッジの数値が正確である
- [ ] 正規化がすべてのケースで機能している

### 8. トラブルシューティング

#### テストが失敗する場合
```bash
# 依存関係を再インストール
rm -rf node_modules package-lock.json
npm install

# テストを再実行
npm run test:ci
```

#### 開発サーバーが起動しない場合
```bash
# ポートが使用中の場合
lsof -ti:3000 | xargs kill -9

# 開発サーバーを再起動
npm run dev
```

#### ビルドエラーが発生する場合
```bash
# キャッシュをクリア
rm -rf .next

# 再ビルド
npm run build
```

